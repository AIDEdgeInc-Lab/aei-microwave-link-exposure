"""Concrete WeatherProvider implementations.

Nothing in ``aei_mw_exposure`` (the core package) imports this subpackage --
providers are opt-in, imported explicitly by whoever needs one, so the core
calculation library stays dependency-free.
"""
